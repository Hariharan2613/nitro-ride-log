import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
import { ArrowUpRight, Search } from "lucide-react";
import { brands } from "@/components/data";
import { PageIntro, Reveal } from "@/components/ui";
export const Route=createFileRoute("/brands")({head:()=>({meta:[{title:"Bike Brands — Veloce"},{name:"description",content:"Explore supported motorcycle brands, popular models and starting prices."},{property:"og:title",content:"Bike Brands — Veloce"},{property:"og:description",content:"Explore supported motorcycle brands, popular models and starting prices."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Brands});
function Brands(){const [query,setQuery]=useState("");const shown=brands.filter(b=>(b.name+b.models).toLowerCase().includes(query.toLowerCase()));return <><PageIntro code="(B) MACHINE CATALOGUE" title="Every marque. One maintenance language." copy="Browse ten of India's most-ridden motorcycle brands, with popular models and reference starting prices."/><section className="section-shell filter-bar"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search brand or model" aria-label="Search bike brands"/><span>{shown.length.toString().padStart(2,"0")} MACHINES</span></section><section className="section-shell brand-grid">{shown.map((b,i)=><Reveal className="brand-card" key={b.name}><div className="brand-card-top"><span className="brand-glyph">{b.mark}</span><span>0{i+1}</span></div><h2>{b.name}</h2><p>{b.models}</p><div><span>STARTING AT</span><strong>{b.price}</strong><ArrowUpRight/></div></Reveal>)}</section><p className="section-shell price-note">Indicative ex-showroom starting prices; verify current local pricing with the manufacturer.</p></>}
